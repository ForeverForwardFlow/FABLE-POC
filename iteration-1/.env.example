# FABLE Environment Configuration
# Copy this file to .env and fill in your values

# ─── AWS Bedrock Configuration ─────────────────────────────────────────────────
# Enable Bedrock for Claude Code CLI
CLAUDE_CODE_USE_BEDROCK=1

# AWS region for Bedrock
AWS_REGION=us-west-2

# AWS credentials are read from ~/.aws/credentials or standard env vars:
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=

# ─── Orchestrator Configuration ────────────────────────────────────────────────
# Maximum turns per worker iteration (1-500, default: 50)
MAX_WORKER_TURNS=50

# Worker timeout in milliseconds (10000-3600000, default: 600000 = 10 min)
WORKER_TIMEOUT_MS=600000

# Maximum Ralph Wiggum loop iterations (1-50, default: 10)
MAX_ITERATIONS=10

# ─── Remote MCP Server Configuration (Optional) ────────────────────────────────
# API key for the deployed greeting MCP server
# MCP_GREETING_API_KEY=your-api-key-here

# URL of the deployed greeting MCP server
# MCP_GREETING_URL=https://mcp-greeting.your-subdomain.workers.dev/mcp

# ─── Cloudflare Workers (for MCP deployment) ───────────────────────────────────
# Set via `wrangler secret put` instead of env vars:
# - MCP_API_KEY: API key for authenticating to deployed MCP servers
# - ALLOWED_ORIGINS: Comma-separated list of allowed CORS origins (optional)

# ─── Development Settings ──────────────────────────────────────────────────────
# Enable verbose logging
# DEBUG=fable:*
